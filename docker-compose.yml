services:
  blog:
    build: .
    container_name: personal-blog
    ports:
      - "3000:3000"
    volumes:
      - blog-data:/data
    environment:
      - NODE_ENV=production
      - DATA_DIR=/data
      - PORT=3000
    restart: unless-stopped

volumes:
  blog-data:
